<%= form_for @ball3 do |t| %>
    <div class="field">
      <%= t.label :body %>
      <%= t.text_field :review3, :size => 140 %>
    </div>
    <%= t.submit "Tweetする" %>
<% end %>
<%= link_to "Tweet一覧に戻る", balls_hama_path %>