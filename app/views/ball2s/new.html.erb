<%= form_for @ball2 do |t| %>
    <div class="field">
      <%= t.label :body %>
      <%= t.text_field :review2, :size => 140 %>
    </div>
    <%= t.submit "Tweetする" %>
<% end %>
<%= link_to "Tweet一覧に戻る", balls_chiba_path %>