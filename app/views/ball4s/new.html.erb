<%= form_for @ball4 do |t| %>
    <div class="field">
      <%= t.label :body %>
      <%= t.text_field :review4, :size => 140 %>
    </div>
    <%= t.submit "Tweetする" %>
<% end %>
<%= link_to "Tweet一覧に戻る", balls_jingu_path %>